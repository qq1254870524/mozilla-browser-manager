# Patches runtime

## rebrowser-patches (source)
Cloned from https://github.com/rebrowser/rebrowser-patches
Path: `runtime/patches/rebrowser-patches/`

Python drop-in driver (already applied patches):
- PyPI `rebrowser-playwright` in `.venv`
- Profile: `--patch rebrowser` / Web UI 选 rebrowser

## rebrowser/ (optional custom chromium binary)
Place `chrome` or `chromium` here if you build a custom patched browser.
